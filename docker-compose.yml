services:
  db:
    image: postgres:15
    container_name: postgres_local
    environment:
      POSTGRES_DB: projeto_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"